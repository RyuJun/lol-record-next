import React from 'react';
import styled from '@emotion/styled';

export const SummonerContainer = styled.div<React.CSSProperties>``;

export const StyledBadge = styled.div<React.CSSProperties>``;
export const IconButton = styled.div<React.CSSProperties>``;
