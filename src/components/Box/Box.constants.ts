import React from 'react';
import styled from '@emotion/styled';

export const BoxContainer = styled.div<React.CSSProperties>`
  max-width: 100%;
  position: relative;
  overflow: visible scroll;
  height: 100%;
`;
